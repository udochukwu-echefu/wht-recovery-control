# Server-only DeepSeek secret used by app/api/intake for receipt extraction.
DEEPSEEK_API_KEY=

# DeepSeek's rolling V4 Flash endpoint currently serves the 0731 release.
DEEPSEEK_MODEL=deepseek-v4-flash
AI_PROVIDER=auto
